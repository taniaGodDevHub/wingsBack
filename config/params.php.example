<?php

return [
    'adminEmail' => 'admin@example.com',
    'senderEmail' => 'noreply@example.com',
    'senderName' => 'Wings',

    'smsRuApiId' => '', // API ID из ЛК SMS.ru → Программистам
    'smsRuFrom' => '', // одобренное имя отправителя (необязательно)
    'jwtSecret' => 'change-me-to-random-secret-key',
    'jwtAccessTtl' => 3600,
    'jwtRefreshTtl' => 86400,
    // TODO: отключить после подключения реальной SMS и доставки email-кодов без возврата в API
    'exposeActivationCode' => true,
    'authCodeEmailFrom' => 'noreply@example.com',
    'authCodeEmailSubject' => 'Код подтверждения',
    'passwordResetEmailSubject' => 'Код для сброса пароля',
    'authChallengeTtl' => 600,
    'authChallengeThrottleSeconds' => 60,

    // DaData: DADATA_API_KEY / DADATA_SECRET_KEY в .env или env сервера (см. .env.example)
    'dadataApiKey' => getenv('DADATA_API_KEY') ?: '',
    'dadataSecretKey' => getenv('DADATA_SECRET_KEY') ?: '',
    'orderDraftTtl' => 1800,
    'paymentBaseUrl' => 'https://pay.example.com',

    // СДЭК API v2 — https://cdekrussia.ru/integration
    // Ключи появятся после заключения договора и создания ЛК СДЭК.
    // 1. Заключить договор на cdekrussia.ru/integration
    // 2. Получить client_id / client_secret в ЛК
    // 3. Заполнить cdekClientId, cdekClientSecret, cdekFromCityCode
    // 4. Протестировать на api.edu.cdek.ru, затем cdekMockMode => false
    // 5. На prod: cdekApiBaseUrl => https://api.cdek.ru
    'cdekMockMode' => false,
    'cdekClientId' => getenv('CDEK_CLIENT_ID') ?: '',
    'cdekClientSecret' => getenv('CDEK_CLIENT_SECRET') ?: '',
    'cdekApiBaseUrl' => 'https://api.cdek.ru',
    'cdekFromCityCode' => 424,
    'cdekDefaultTariffCode' => 136,
    'cdekDefaultPackageWeightGrams' => 500,
    'cdekCreateOnConfirm' => false,
];
